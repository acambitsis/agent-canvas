# Convex Configuration
# Get these from your Convex dashboard after running `npx convex dev`
CONVEX_DEPLOYMENT=your-deployment-name
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# WorkOS Configuration
# Get these from your WorkOS dashboard at https://dashboard.workos.com
WORKOS_API_KEY=sk_live_xxxxxxxxxxxxx
WORKOS_CLIENT_ID=client_xxxxxxxxxxxxx
# Session encryption key - MUST be at least 32 characters
# Generate with: openssl rand -hex 32
WORKOS_COOKIE_PASSWORD=your-32-character-secure-string-here

# WorkOS Redirect URI (where users return after auth)
# Must match the redirect URI configured in WorkOS dashboard
WORKOS_REDIRECT_URI=http://localhost:3000/api/auth/callback

# WorkOS Webhook Secret (for org membership sync)
# Get this from WorkOS dashboard when setting up webhooks
WORKOS_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# WorkOS Widgets Configuration
# IMPORTANT: For WorkOS Widgets to work, you must add your origin to the
# allowed origins list in WorkOS Dashboard > Authentication > Allowed Origins
# Add: http://localhost:3000 (dev) and your production URL

# Super Admin Configuration
# Comma-separated list of email addresses with super admin privileges
SUPER_ADMIN_EMAILS=admin@example.com

# Application Configuration
BASE_URL=http://localhost:3000
